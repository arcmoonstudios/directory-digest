name: Secret Scan

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]

jobs:
  gitleaks-scan:
    runs-on: ubuntu-latest
    name: Scan code for secrets using Gitleaks
    steps:
      - uses: actions/checkout@v4
      - name: Run Gitleaks (scan)
        uses: gitleaks/gitleaks-action@v2.1.0
        with:
          job-args: '--no-git'  # scan the workspace only and don't require git history
